<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vicmarket">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VicMarket">
        <activity android:name=".activities.ThanhToanActivity"></activity>
        <activity android:name=".activities.ViewAllActivity" />
        <activity android:name=".activities.ChiTietSanPhamActivity" />
        <activity android:name=".authen.RegisterActivity" />
        <activity android:name=".authen.LoginActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.VicMarket.NoActionBar" />
        <activity android:name=".Onbroading.OnbroadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>